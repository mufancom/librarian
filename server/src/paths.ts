import * as Path from 'path';

export const PROJECT_DIR = Path.join(__dirname, '..');

export const PUBLIC_DIR = Path.join(PROJECT_DIR, 'public');

export const CONVENTION_GIT_BASE_PATH = Path.join(PUBLIC_DIR, 'convention');
